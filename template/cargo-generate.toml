[template]
cargo_generate_version = ">=0.19.0"

[placeholders.gh-username]
type = "string"
prompt = "GitHub username (or organization)"
regex = "(^[A-Za-z0-9]$)|(^[A-Za-z0-9][A-Za-z0-9-]{0,37}[A-Za-z0-9]$)"

[placeholders.description]
type = "string"
prompt = "Project description"
